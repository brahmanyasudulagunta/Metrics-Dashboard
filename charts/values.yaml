backend:
  image: ashrith2727/backend-metrics
  tag: "v1"
  replicas: 1
  adminPassword: admin
  jwtSecret: supersecretkey
  devMode: false

frontend:
  image: ashrith2727/frontend-metrics
  tag: "v1"
  replicas: 1

prometheus:
  url: http://prometheus-server:9090

persistence:
  enabled: true
  size: 1Gi
  storageClass: ""

ingress:
  enabled: false
  host: metrics.example.com
  className: nginx

serviceAccount:
  create: true
  name: ""

monitoring:
  grafana:
    enabled: false            # our app IS the dashboard, no need for Grafana
  prometheus:
    prometheusSpec:
      retention: 7d
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          memory: 512Mi
  alertmanager:
    enabled: false            # can enable later if needed
  nodeExporter:
    enabled: true
  prometheus-node-exporter:
    service:
      port: 9101              # avoid conflict with default 9100
      targetPort: 9101
    hostPort:
      enabled: false          # don't bind to host port — prevents scheduling conflicts
  kubeStateMetrics:
    enabled: true
