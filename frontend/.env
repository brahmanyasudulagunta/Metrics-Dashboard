# ========================================
# Metrics Dashboard - Frontend Environment
# ========================================

# The URL of the FastAPI backend. Leave empty to use relative paths (e.g., when hosted behind the same Nginx/Ingress).
# For local frontend development running on port 3001, point this to the backend port.
VITE_API_URL=http://localhost:8000
